# Task TODO

- [ ] Add a “Current date” element into the UI (footer or profile/dashboard) and format it nicely.
- [ ] Update JS to populate the element with `new Date().toLocaleDateString(...)`.
- [ ] Ensure the script runs on DOMContentLoaded and doesn’t break existing date logic.
- [ ] (Optional) Add lightweight CSS for a nice look if needed.

